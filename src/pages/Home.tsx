import Table from "../components";

const Home = () => {



    return (
        <div>
            {/* {data && <Table data={data} params={params} setParams={setParams} />} */}
            <Table />
        </div>
    );
};

export default Home;
